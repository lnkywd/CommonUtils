<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="common.utils">

    <application
        android:name="common.test.MyApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_camera"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/MainTheme">

        <activity
            android:name="common.test.MainActivity"
            android:screenOrientation="portrait">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="common.test.TestActivity" />
        <activity
            android:name="common.test.JumpActivity1"
            android:exported="false"
            android:screenOrientation="portrait" />
        <activity
            android:name="common.test.Test5Activity"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="portrait" />
        <activity android:name="common.test.Test6Activity" />
        <activity android:name="common.test.Test7Activity" />

    </application>

</manifest>